# 真实阴影算法（S2 = 裁剪后蒙版映射到投影平面）

等距视角（Isometric）2D 游戏《冒险村物语》建筑的真实阴影投射算法。
最初在 `sunshine.html` 调试页逐步推演得到，后同步到 `sprites.js` 的 `SpriteKit.drawGroundShadow`。
本文件是该方法的完整英文对照总结与实现说明。

---

## 1. 背景：等距坐标系

- 瓦片尺寸：`TILE_W = 84`，`TILE_H = 42`（等距菱形瓦，2x2 建筑底面菱形）。
- 阴影光源在**左下** → 阴影投向往**右上方**，沿等距对角线方向。
- 投影方向向量：`(dx, dy) = (projX, -projX / 2)`，其中 `projX = 140`（投影距离，固定）。
- `groundY` = 建筑底面菱形**下角尖**（底角）所在的基准线。

---

## 2. 底面菱形四角（锚点）

设建筑底面菱形中心 `cx`、底角基准 `groundY`，则菱形四角为：

| 记号 | 含义 | 坐标 |
|------|------|------|
| `L` | 左角（= 底部轮廓左端点 X1） | `(cx - TILE_W, groundY - TILE_H)` |
| `B` | 下角尖（= 底部轮廓中点 X4） | `(cx, groundY)` |
| `R` | 右角（= 底部轮廓右端点） | `(cx + TILE_W, groundY - TILE_H)` |
| `T` | 上角 | `(cx, groundY - 2*TILE_H)` |

> 建筑贴图经"底边精确贴合"缩放后，其底部非透明轮廓的左端点、右端点、中点分别精确落在 `L`、`R`、`B` 上（`L` 即 `X1`，`B` 即 `X4`）。

---

## 3. 投影平面四边形（阴影目标平面）

把底面菱形 `L` 与 `B` 两点沿投影方向平移 `(dx, dy)`，得到投影平面四角：

| 记号 | 含义 | 坐标 |
|------|------|------|
| `X1` | 原位 = 左角 `L` | `(cx - TILE_W, groundY - TILE_H)` |
| `X4` | 原位 = 下角尖 `B` | `(cx, groundY)` |
| `X2` | `L` 的投影 | `X1 + (dx, dy)` |
| `X3` | `B` 的投影 | `X4 + (dx, dy)` |

这四个点围成一个**平行四边形** `X1 → X4 → X3 → X2`，这就是阴影的**目标平面 / 约束平面**。
阴影一定落在该四边形内，且四边形随投影距离实时变化（为日出日落做准备：角度/距离变化只需改 `dx`）。

---

## 4. 蒙版与裁剪（L1 线裁剪）

- 蒙版：建筑贴图的不透明像素位图 `baseMask`，尺寸固定 `160 × 160`（`SpriteKit.MASK_W/H`）。
- 绘制时建筑缩放因子 `sc = dw / 160`（`dw` = 建筑绘制宽，底边精确贴合后可能 ≠ 160）。
- 蒙版像素 `(mx, my)` 到屏幕坐标：
  ```
  px = cx - dw / 2 + mx * sc
  py = topY + my * sc      （topY = groundY - ratio * dh，ratio = img.bottomRatio）
  ```
- **L1 线**：连接 `X1(L)` 与 `R(右角)` 的底边线，`y = groundY - TILE_H`（与 `X1.y` 相同）。
- 裁剪：**去掉 L1 线下方（`py >= l1Y`）的下半部**，剩余部分即"可投阴影的蒙版形状"。
- 求剩余裁剪蒙版的包围矩形 **S1**：
  ```
  S1 = { x: rMinX, y: rMinY, w: rMaxX - rMinX, h: l1Y - rMinY }
  ```
  下边贴合 `L1`，上边 = 剩余蒙版最小 y（`rMinY`），左右 = 蒙版最左/最右像素。
  S1 上边两角：`X5 = (rMinX, rMinY)`（左上）、`X6 = (rMaxX, rMinY)`（右上）。

---

## 5. 角点映射关系（用户定义的核心规则）

把裁剪后的**蒙版形状**（不是整个 S1 矩形）映射到投影四边形上：

| S1 / 蒙版 | → | 投影四边形 |
|-----------|----|-----------|
| S1 左下（`X1` 点） | → | `X1`（`L`） |
| S1 右下（`R` 点） | → | `X4`（`B`） |
| S1 左上（`X5`） | → | `X2`（`L'`） |
| S1 右上（`X6`） | → | `X3`（`B'`） |

---

## 6. 逐像素双线性映射（S2 = 真实阴影）

对**每一个裁剪后蒙版像素**（`py < l1Y` 且蒙版不透明）：

1. 归一化：
   ```
   u = (px - rMinX) / (rMaxX - rMinX)     // 0=左, 1=右
   v = (py - rMinY) / (l1Y - rMinY)       // 0=顶(S1.上边), 1=底(L1)
   ```
2. 顶边与底边插值：
   ```
   top(u) = X2 + (X3 - X2) * u     // X2→X3
   bot(u) = X1 + (X4 - X1) * u     // X1→X4
   ```
3. 按 v 在顶边/底边之间插值，得目标点：
   ```
   dst = top(u) + (bot(u) - top(u)) * v
   ```
4. 按**蒙版行**处理：每行的不透明像素按**连续段**分组。同一蒙版行 v 相同 →
   投影后 y 相同，若按行连折线则每条都是水平细线（闭合后面积≈0，`fill()` 填不出
   像素）。因此对每个连续段，把段起点/终点分别映射本行（v0）与下一行（v1），
   构成一个**条带四边形**：`dst(uL,v0) → dst(uR,v0) → dst(uR,v1) → dst(uL,v1)`。
   所有条带放入同一 path，`ctx.fill()` 一次性填充，得到**保留了建筑蒙版真实形状**
   的阴影（非实心平行四边形；段间有孔洞处不连接，保留蒙版镂空形状）。

> 结果即粉色区域 **S2**：它是沿 L1 裁剪后的蒙版形状，经双线性映射到投影平面四边形上，
> 角点严格对齐 X1→X1、R→X4、X5→X2、X6→X3，并随 X1..X4（投影距离）实时变化。

---

## 7. 退化为原逻辑的兜底

当没有蒙版 / 底边轮廓数据（`img.baseMask` 或 `img.bottomProfile` 缺失）时，直接画整个投影四边形
`X1 → X4 → X3 → X2` 填充黑色（保持原行为，避免漏阴影）。

---

## 8. 实现位置

| 文件 | 作用 |
|------|------|
| `sprites.js` | `SpriteKit.drawGroundShadow(...)` —— 算法核心（蒙版形状 → 条带四边形映射）；`SpriteKit.Shadow.draw(ctx, entity)` —— 面向对象统一入口 |
| `sunshine.html` | 调试页 S2 区块 —— 粉色（`#ff69b4`）标注真实阴影，右侧按钮可开关，便于视觉校验 |
| `game.js` | 所有实体绘制处统一调用 `SpriteKit.Shadow.draw(ctx, { img, x, groundY, tileW, w, h, projX })` |

---

## 9. 统一阴影系统（面向对象 · 解耦）

任何"可投影实体"（建筑 / 装饰 / 人物 / 怪物）只需提供一个描述对象，
`SpriteKit.Shadow.draw` 即用**同一套算法**生成形状阴影：

```js
SpriteKit.Shadow.draw(ctx, {
  img,        // 贴图（须 setup 过：有 bottomProfile + baseMask）
  x,          // 实体中心 x
  groundY,    // 地面基准（底部贴地线 y）
  tileW,      // 底面菱形半宽：建筑 84，装饰/人物/怪物单格 42
  w, h,       // 实体实际绘制宽/高（人物底边窄，必须显式传，不能套底边贴合公式）
  projX,      // 投影距离（沿 X 轴）：建筑 140 / 装饰 100 / 人物 45 / 怪物 40
  shrinkX,    // 横向收缩系数（缺省 1）：人物/装饰/怪物配置默认 0.03
});
```

- 蒙版自动装配：`ALIGN_KEYS`（有底边数据）+ `SHADOW_KEYS`（建形状蒙版）。
  建筑用 160×160 正方形蒙版（与像素遮挡共用）；装饰/人物/怪物按原始宽高比建蒙版，
  长边等比缩到 ≤160（防止大立绘每帧遍历几十万像素卡顿）。
- 人物 / 怪物阴影替换了旧的手绘椭圆（`ctx.ellipse`），全部统一为蒙版形状阴影。
- 阴影渲染在实体本体之前（先阴影后实体），投影方向统一为等距对角线（右上方）。

### 横向收缩（2.5D 视角）

建筑占满 2×2 单元格 → 菱形半宽 84 合理。人物/装饰/怪物**不填满单元格**，
若仍按单元格菱形（42）投影会显得"阴影太宽"。2.5D 的"横向" = 屏幕水平
（左上↔右下 = 菱形左角↔右角方向），"竖直" = 左下↔右上（投影方向）。

因此 `shrinkX` 只**收缩菱形半宽**（`X1.x = cx - tw * shrinkX`，建筑缺省 1），
投影位移 `dx` 保持不变——阴影变窄但方向/长度不变。人物/装饰/怪物配置默认 `0.03`（极窄）。

### 双层缓存：缓存1（真实阴影） + 缓存2（个性定制）

| | 缓存1：真实阴影条带 | 缓存2：静态配置 |
|---|---|---|
| 内容 | 蒙版→L1裁剪→条带（归一化 `[uL,uR,v0,v1]`） | 每个精灵/元素的横向收缩系数 |
| 存储 | `SpriteKit._shadowCache`（内存） | `shadow_config.json`（静态文件） |
| 生成时机 | **实体创建时**预热（`Shadow.prepare`，耗系统资源） | 工具页导出；游戏**启动时静态读取**（`Shadow.loadConfig`） |
| 键 | `img.src\|w\|h\|tileW\|projX`（与位置无关） | 元素 key（`inn`/`tree`/`ck_knight`/`slime`…） |
| 作用 | 算出"贴近真实"的阴影几何，运行期直接复用 | 个性定制 + 修复空间，省系统资源 |

- `buildShadowGeo(img, w, h, tw, px)`：纯计算（占位 `cx=0, groundY=0`，u/v 归一化抵消位置），
  缓存1 的构建核心；`Shadow.geo(e)` 查/懒生成；`Shadow.prepare(e)` 实体创建时预热。
- `Shadow.draw(ctx, e)`：渲染时**直接读缓存2**（`e.key` 查 `config.shrinkX`），
  再按当前 X1..X4 对缓存1 条带做少量插值 + `fill()`。
- game.js 包装了 `makeBuilding / makeAdventurer / makeSlime`，并在装饰放置处调用
  `prepareShadow(key, group)`——**任何新元素创建即预热缓存1**。

### 配置工具页 shadow_config.html

复制 sunshine 调试页风格的工具：
- 列出全部 27 个元素（建筑/装饰/人物/怪物，按贴图 key）
- 点选元素 → 画布预览（等距地面 + 阴影 + 本体）
- **横向收缩滑块**实时微调（0.01~1.00），写入 `Shadow.config.shrinkX`
- 导出/复制 `shadow_config.json`（覆盖项目文件即生效，游戏启动静态读取）
- 一键重置默认（建筑 1，其余 0.03）

### S2 归一化条带缓存

蒙版 → L1 裁剪 → 连续段条带的计算与实体位置 `cx/groundY` 无关（u/v 已归一化 0~1），
只需在贴图 / 绘制尺寸 / 菱形半宽 / 投影距离不变时算一次，存入 `SpriteKit._shadowCache`：

- 缓存键：`img.src | dw | dh | tw | px`
- 缓存值：`{ deg, strips: [[uL,uR,v0,v1], ...] }`
- 每帧渲染仅按当前 `X1..X4`（含横向收缩）对条带做少量插值 + `fill()`，
  不再重复遍历蒙版像素（实测帧率约翻倍：72 → 147 FPS）。
- 画布外区域的裁剪由 canvas `fill()` 自动处理，缓存构建时无需逐像素过滤。

---

## 10. 关键要点回顾（防上下文丢失）

1. **阴影平面 = 平行四边形 X1→X4→X3→X2**，不做超出、不做整体裁剪。
2. **真正投的形状 = 沿 L1 裁剪后的蒙版形状**，不是 S1 矩形框，也不是整张蒙版。
3. **L1 裁剪**：去掉 `py >= l1Y`（`l1Y = groundY - TILE_H`）的下半部。
4. **角点映射**：X1→X1、R→X4、X5→X2、X6→X3（顺序不可乱）。
5. **双线性插值**：`u` 走左右、`v` 走上下（v=0 顶边=X2X3、v=1 底边=X1X4）。
6. **按行连续段 → 条带四边形 + fill**：每行段映射为条带四边形（本行 v0 + 下一行 v1），
   保留蒙版形状（含镂空孔洞），非实心矩形。⚠️ 不要按行连折线——同行 v 相同投影后 y 相同，
   折线是水平细线，`fill()` 面积≈0 会画不出阴影（曾因此导致游戏阴影消失）。
7. **实时跟随**：S2 随 X1..X4 变化，改 `dx`（投影距离/角度）即可模拟日出日落。
